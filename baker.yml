---
name: baker-test
vagrant:
  box: "ubuntu/xenial64"
  memory:
    prompt: "how much memory?"
  network:
    - forwarded_port:
        guest: 3000
        host: 
          prompt: "Port for express server?"
    - private_network:
        ip: 192.168.33.33
  synced_folders:
    - folder:
        src:
          prompt: "What host folder to sync for projects?"
        dest: "/code"

bake:
  ansible:
    source: roles/
    playbooks:
      - node.yml
      - python.yml