---
name: baker-test
vagrant:
  box: "ubuntu/trusty64"
  memory:
    prompt: "how much memory?"
  network:
    - forwarded_port:
        guest: 3000
        host: 
          prompt: "Port for express server?"
    - private_network:
        ip: 192.168.22.22
bake:
  ansible:
    source: deployment/
    playbooks:
      - node.yml
      - python.yml
